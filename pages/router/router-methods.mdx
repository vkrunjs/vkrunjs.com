---
title: "Router Methods"
description: 'The "Router Methods" page details the HTTP methods supported by the VkrunJS router, including "GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS", and "HEAD". Each method is explained with examples to demonstrate how to define routes that handle specific HTTP requests, ensuring clear and structured application routing.'
---

## Router - Methods

A Router method corresponds to an HTTP method and is attached to an instance of the VkrunJS router.

| Method            | Description                                                                 |
|-------------------|---------------------------------------------------------------------------|
| `get`     | Handles GET requests. Used to retrieve data.                               |
| `post`    | Handles POST requests. Used to create new resources.                       |
| `put`     | Handles PUT requests. Used to replace existing resources.                  |
| `patch`   | Handles PATCH requests. Used to partially modify a resource.               |
| `delete`  | Handles DELETE requests. Used to delete resources.                         |
| `options` | Handles OPTIONS requests. Used to retrieve communication options.          |
| `head`    | Handles HEAD requests. Used to retrieve only the headers without the body. |

Example:

```ts
import Vkrun, { Router, Request, Response } from 'vkrun'

const vkrun = Vkrun()
const router = Router()

router.get('/', (req: Request, res: Response) => {
  res.status(200).send('GET method route')
})

router.post('/', (req: Request, res: Response) => {
  res.status(200).send('POST method route')
})

router.put('/', (req: Request, res: Response) => {
  res.status(200).send('PUT method route')
})

router.patch('/', (req: Request, res: Response) => {
  res.status(200).send('PATCH method route')
})

router.delete('/', (req: Request, res: Response) => {
  res.status(200).send('DELETE method route')
})

router.options('/', (req: Request, res: Response) => {
  res.status(200).end('OPTIONS method route')
})

router.head('/', (req: Request, res: Response) => {
  res.status(200).end() // The HEAD method does not send data in the response body
})

vkrun.use(router) // Inject all created routes

vkrun.server().listen(3000, () => {
  console.log('VkrunJS started on port 3000')
})
```

Equivalent:

```ts
import Vkrun, { Request, Response } from 'vkrun'

const vkrun = Vkrun()

vkrun.get('/', (req: Request, res: Response) => {
  res.status(200).send('GET method route')
})

vkrun.post('/', (req: Request, res: Response) => {
  res.status(200).send('POST method route')
})

vkrun.put('/', (req: Request, res: Response) => {
  res.status(200).send('PUT method route')
})

vkrun.patch('/', (req: Request, res: Response) => {
  res.status(200).send('PATCH method route')
})

vkrun.delete('/', (req: Request, res: Response) => {
  res.status(200).send('DELETE method route')
})

vkrun.options('/', (req: Request, res: Response) => {
  res.status(200).end('OPTIONS method route')
})

vkrun.head('/', (req: Request, res: Response) => {
  res.status(200).end() // The HEAD method does not send data in the response body
})

vkrun.server().listen(3000, () => {
  console.log('VkrunJS started on port 3000')
})
```